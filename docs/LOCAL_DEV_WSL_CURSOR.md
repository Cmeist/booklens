# Local Development: Windows 11 + WSL2 + Cursor

## Overview

BookLens should be developed inside WSL2 Ubuntu, with Cursor connected to the WSL folder.

Do not develop directly inside `/mnt/c/...`.

Recommended repo location:

```bash
~/dev/booklens




which git
which node
which npm
which python3
which uv
